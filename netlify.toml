# Publica solo la landing final; los archivos fuente/editor y el material crudo no se suben al sitio público
[build]
  publish = "."
  command = "rm -rf uploads 'Landing Certificacion LSP.dc.html' landing-certificacion-lsp.html support.js image-slot.js .image-slots.state.json"
